@model IEnumerable<MVC網站.Models.Location>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LocationName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AreaCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LocationName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AreaCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LocationSerial }) |
            @Html.ActionLink("Details", "Details", new { id=item.LocationSerial }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LocationSerial })
        </td>
    </tr>
}

</table>
